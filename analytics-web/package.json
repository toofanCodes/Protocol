{
  "name": "protocol-analytics",
  "version": "1.0.0",
  "description": "Analytics dashboard for Protocol habit tracking",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "dependencies": {
    "chart.js": "^4.4.0",
    "dexie": "^3.2.4",
    "sql.js": "^1.10.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
